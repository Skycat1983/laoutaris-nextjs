# Production-Readiness Risks

This is the canonical tracker for known production risks. Add risks when a gap is
found and remove them only when the mitigation is complete and verified.

| ID | Severity | Area | Risk | Mitigation Status | Owner |
| --- | --- | --- | --- | --- | --- |
| R-001 | High | Shopify | Full checkout/cart handoff remains undecided after the first-release enquiry handoff. | Partially mitigated: T-008 replaced the product detail `Add to Cart` placeholder with a contact enquiry link and documented that real checkout/cart still requires variant, line-item, and ownership decisions. T-062 carries queried variant IDs, titles, availability, price money, compare-at price money, and optional variant images through product DTOs. T-208 now uses Shopify's hosted public product URL for available products when Shopify exposes a valid `onlineStoreUrl`, while keeping app-owned cart/checkout ownership, variant selection, line-item construction, payment, shipping, refund, and fulfilment behavior separate. | Unassigned |
| R-002 | High | Auth/Admin | Broader admin guard/logging cleanup and destructive admin workflow protections remain open. | Partially mitigated: T-002 resolved F-042 credentials role persistence, T-003 resolved F-043 stable ownership helpers, T-005 added a route-local guard to the Cloudinary signing endpoint, T-009 removed registration credential logs, T-013 repaired the active credentials sign-in form while removing `SignInFormBackup`, T-016 removed bcrypt helper credential logging while verifying bcrypt 6 hash compatibility, T-019 removed the stale custom login/session path, duplicate/test-header session helpers, and `/protected`, T-026 added shared user/admin API route guards for a representative protected route slice, T-027 migrated user navigation/favourites/watchlist read routes to the shared user guard, T-028 added favourite/watchlist action DB/revalidation ownership, T-033 migrated user comment delete to the shared user guard before DB/transaction work, T-039 migrated admin read routes to the shared admin guard with focused coverage, T-040 migrated admin delete routes to the shared admin guard before destructive work, T-041 fixed middleware protected API `401` behavior while removing direct middleware debug logs, T-042 migrated user comment GET/POST/PATCH to the shared user guard before body, DB, model, or transaction work, T-043 locked the protected API guard invariant with static coverage, T-056 aligned optional persisted passwords with OAuth/credentials behavior, T-138 documented admin bootstrap, promotion, lockout recovery, evidence handling, and verification in the auth runbook, T-141 blocks current-admin self-deletion and last-admin deletion, T-156/T-163/T-164/T-165 added destructive delete previews, evidence gates, and redacted audit receipts before mutation, and T-273 verifies the persisted MongoDB admin role before rendering the admin dashboard frontend shell so stale admin JWTs cannot view dashboard content after demotion. Production logging policy remains under R-019. | Unassigned |
| R-003 | High | Deployment | Production environment variable inventory, ownership, rotation guidance, and legacy/platform variable decisions are incomplete. | Partially mitigated: T-065 updated the environment runbook from source-search evidence without secret values or runtime config changes. T-086 refreshed URL variable status after the shop loader and `/project` redirect cleanup: `NEXT_PUBLIC_BASE_URL` is now deprecated current-source configuration, and `VERCEL_URL` no longer owns the current `/project` redirect. T-087 removed the retired server wrappers that were the remaining current-source users of `VERCEL_ENV`/`VERCEL_URL` URL construction, and the environment runbook now classifies those Vercel names as not required by current source. Active runtime variables, smoke-script variables, platform variables, and legacy/deprecated candidates now have classification, required-environment, owner/status, and known rotation/configuration notes. Credential rotation and owner-managed cleanup decisions remain separate. | Unassigned |
| R-004 | Medium | Security | CSP and API CORS policy are broadly permissive for the production admin, auth, and commerce surface. | Partially mitigated: T-096 removed the global API CORS header rule from `next.config.mjs`, eliminating the wildcard-origin plus credential pairing and wildcard allowed request headers, and added `X-Content-Type-Options`, `Referrer-Policy`, conservative `Permissions-Policy`, and low-risk CSP `object-src`, `base-uri`, `form-action`, and `frame-ancestors` directives. Full strict CSP allowlisting, HSTS, dynamic route-level CORS, CSP reporting, and monitoring remain separate. | Unassigned |
| R-005 | Medium | Quality | Test coverage is narrow for APIs, auth, Shopify, SSR/data-fetching, deployment smoke checks, transforms, admin flows, and input persistence boundaries. | Partially mitigated: T-010 added focused public enquiry route tests, T-011 restored the full Jest baseline after the Mongoose update, T-012 added focused user comment create/update route tests, T-013 added focused sign-in form tests, T-016 added focused bcrypt helper compatibility tests, T-017 added focused subscription server-action tests, T-018 added artwork list service/API/loader no-self-fetch tests, T-020 added focused admin collection write tests, T-021 added focused public search service/API/page tests, T-026 through T-050 expanded representative API guard/status/response-helper coverage, T-051/T-052 added transform and artwork image coverage, T-056 added credentials/OAuth password-contract coverage, T-060/T-062/T-063 added Shopify component/transform coverage, T-067 through T-095 expanded source-hygiene and route-loader coverage, T-141 added admin current-admin/last-admin delete guard coverage, T-142 added admin article/collection relationship existence coverage, T-144 added admin collection form error-surface and fetcher structured-error coverage, T-145 added `/artwork` page/API query parity coverage, T-146 added sorted blog loader/view/continuous-loading coverage, T-147 added main-nav fallback coverage, T-148 added admin article/blog form error-surface coverage, T-150 added artwork/product-link form error coverage, T-151 added current-scope search metadata/no-results coverage, T-152 added admin archive entry-point coverage, T-153 added blog pinned/tag form and read-filter coverage, T-154 added public taxonomy section validation coverage, T-155 added visible breadcrumb behavior/source coverage, T-156/T-163/T-164 added destructive admin delete preview, UI gating, fetcher, route evidence, and confirmation coverage, T-165 added audit-event persistence and no-mutation-on-audit-failure coverage, T-166/T-167 added prototype width/source coverage for the expanded homepage prototype, T-168 completed targeted prototype visual QA, T-170 added semantic style-map source/invariant coverage, T-171/T-173 added read-only data and admin read-list audit evidence, T-174 added admin read-list query-bound route coverage, T-178 added comment/user delete entry-point coverage, T-175 added blog read-list pagination coverage, T-176 added route-backed blog filter coverage, T-177 added route-backed blog search coverage, T-179 added collection read-list pagination/search coverage, T-180 added article read-list pagination/filter/search coverage, T-181 added artwork read-list pagination/filter/search coverage, T-182 added comment/user read-list pagination coverage, T-183 preserved the admin read-list pagination suite after extracting the shared control, T-184 categorized strict TypeScript `noEmit` failures as test-only cleanup debt, and T-185 removed the stale public navigation DTO fixture group. Remaining high-risk coverage follows public-search scope, compliance, monitoring, and deployment. | Unassigned |
| R-006 | High | Data/API | API response envelopes, HTTP statuses, route/fetcher parity, validation, field contracts, transform outputs, and public-safe error handling are inconsistent across route groups. | Partially mitigated: T-004 standardized the public single Shopify product route, T-010 hardened public enquiry validation/errors, T-012 hardened user comment create/update validation/status/DTO behavior, T-017 hardened subscription action validation and public-safe failure messages, T-020/T-034/T-037/T-038 hardened admin collection/article/artwork/blog create/update validation, T-021/T-035/T-036 hardened public search/artwork/shop query parsing, T-029 through T-032 resolved route/fetcher parity drift, T-039/T-040 hardened admin read/delete guards and ID behavior, T-044 through T-050 applied shared response helpers across protected user, public content/navigation, and admin routes, T-051/T-052 resolved focused public transform/artwork image contracts, T-053 through T-056 resolved the scoped article/blog/user field-contract matrix, T-062/T-063 preserved queried Shopify product metadata, T-142 added route-local artwork existence checks for admin article and collection relationship writes, T-144 preserved structured error-envelope fields through the client fetcher for admin form use, T-145 aligned `/artwork` page query parsing with the shared public artwork schema, T-151 added current-scope public search result metadata for articles, blogs, and collections, T-154 added route-local runtime validation for public article and collection section inputs before service calls, T-165 added a server-only redacted admin delete audit model/helper plus public-safe no-mutation behavior when audit creation fails, T-174 added bounded shared pagination parsing for admin read-list routes, T-176 moved admin blog read filters to route-backed query params before counting/pagination, T-177 added bounded admin blog search over title/slug before counting/pagination, T-179 added bounded admin collection search over title/slug before counting/pagination, T-180 added bounded admin article search plus route-backed article filters before counting/pagination, and T-181 added bounded admin artwork title search plus constrained route-backed artwork filters before counting/pagination. T-182 consumed existing comment/user route metadata without changing route contracts. Remaining A-017/A-018 follow-up includes public search scope decisions, collection section launch policy, and broader F-015/F-049/F-050/F-053 work. | Unassigned |
| R-007 | Medium | Operations | MongoDB backup, migration, destructive admin-delete, and rollback procedures are not fully documented. | Partially mitigated: T-149 added the admin content operations runbook and documented that production destructive deletes require cascade preview, backup/export evidence, owner/delegated review evidence, and redacted audit evidence. T-156 added read-only preview routes and a typed cascade-preview contract for current admin delete resources without changing destructive execution. T-163 renders that preview in admin delete confirmations and blocks confirmation while preview loading, failed, or blocked. T-164 requires backup/export and owner/delegated review evidence in the admin delete UI and validates it at each delete route before destructive mutation. T-165 persists redacted audit events before destructive mutation and blocks mutation if that audit receipt cannot be created. T-172 documented how operators verify redacted admin delete audit receipts using safe fields and placeholder-only query shapes. Remaining operations risk is the broader owner-approved production procedure for backup, restore, rollback, and evidence handling, not the missing delete-audit control. | Unassigned |
| R-008 | High | Assets/Security | Cloudinary upload signing, asset ownership, and lifecycle policy are not production-ready. | Partially mitigated: T-005 added admin/API guard, request validation, missing-secret handling, and tests for the signing route. T-052 sanitized public artwork image DTOs and tightened persisted color validation. T-065 documented Cloudinary environment ownership/status. T-066 restricted signed upload params to `timestamp`, `upload_preset: "laoutaris_art"`, and `source: "uw"` for the current admin widget. T-101 documented the interim lifecycle policy: preserve assets on MongoDB deletion, keep automatic Cloudinary cleanup disabled until backup/restore and owner approval exist, use manual orphan-review evidence before deletion, keep the current hard-coded upload preset until owner decision, reject signed folders until folder policy is approved, and prefer Cloudinary-managed or explicitly allowed-host blog/collection images. T-135 enforces that allowed-host policy for admin article, blog, and collection image URL writes. T-136 centralized current app-authored Cloudinary delivery transformations behind a shared helper. T-171 audited existing article, blog, and collection image URLs and found all checked values already use the configured Cloudinary delivery path, so no scoped image URL migration is indicated. Remaining runtime work includes owner-approved cleanup implementation, upload-result metadata hardening, and resolving split cloud/preset/folder configuration. | Unassigned |
| R-009 | Low | Docs | Historical root shop notes are useful but not canonical. | Open: F-035 keeps consolidation pending before deletion. | Unassigned |
| R-010 | High | Architecture | Architecture refactor scope, ownership boundaries, and scalable patterns are now audited but not implemented. | Partially mitigated: T-007, T-018, T-021, T-070, T-071, T-072, T-073, T-074, T-075, T-076, T-077, T-078, T-079, T-080, T-081, T-083, T-084, and T-085 proved the ADR 0004 server-only data-service pattern for artwork detail, artwork list, public search, collection navigation, article navigation reads, collection redirect pages, populated article detail data, blog detail data, blog list data, `ArtworkLoader` detail data, collection artwork list/detail data, biography article list data, collection section list data, account navigation data, account saved-artwork data, account profile/comment data, and public shop product listing data. T-086 removed the user-facing hard-coded same-app origin slice from logout navigation, mobile auth links, and the `/project` redirect. T-087 removed the retired server-side same-app API wrapper layer after route-critical callers were migrated. T-137 added a recursive static client runtime import-graph guard and cleaned current client/server import-boundary violations without an allowlist. Route-builder ownership, root-layout session/cache ownership, and staged source pruning remain open. | Unassigned |
| R-011 | Medium | Code health | Unused code, dependencies, WIP modules, and superseded patterns may obscure production risks. | Partially mitigated: T-019 pruned the legacy auth/session route slice from F-032, T-022 removed confirmed-unused direct dependencies plus the unused `core-js` lockfile path, T-087 removed the retired server-side same-app API wrapper entrypoints plus stale account page import/commented code, and T-095 removed stale commented `console.log()` snippets from scoped source while adding full-source source hygiene. A-014 staged source pruning remains open for F-031. | Unassigned |
| R-012 | High | Rendering | Server-side rendering and data-fetching patterns rely on self-HTTP, unclear cache policy, and global root layout DB/session work. | Partially mitigated: T-007, T-018, T-021, T-070 through T-085, and T-087 removed many same-app HTTP paths. T-023 removed credentials password verification/native bcrypt from the normal public-page import path. T-102 removed global root-layout DB/session work and short-circuited public middleware before token parsing; build now shows several shell-only public routes as static. T-110 documented the public route rendering/cache matrix and made remaining route-local dynamic public pages explicit with `force-dynamic` segment config. T-231 narrowed middleware matching to protected frontend/API prefixes only. A-022 confirmed the remaining high-value Next.js efficiency gap is controlled public caching/ISR, cached non-`fetch` service reads, static params where freshness allows, and explicit deploy-vs-revalidate freshness for sitemap/default redirects. T-232 through T-251 completed the accepted freshness matrix and the low-risk public cache proofs for biography, collections, sitemap, blog primary/detail/list slices, sorted blog pages, and fixed unfiltered `/artwork` browse pages 1-5. T-252 paused F-111 runtime cache expansion, T-253 scoped the root modal/provider split, T-254 split unused modal-provider language state plus lazy-loaded the root modal dialog host, and T-256 lazy-loaded the public account navigation island while preserving global session and modal ownership. Broad static/ISR migration beyond those route-family proofs, account/admin cache policy, deeper root provider ownership changes, and full data freshness decisions remain open. | Unassigned |
| R-013 | High | Quality | Testing has been attempted but is not yet proven as a reliable safety net for production refactors. | Open: A-006 complete; F-003, F-016, F-018, and F-019 routed to the testing workstream. T-015 now defines the future Next major migration verification baseline before framework package edits, including focused auth/admin/public archive/shop tests, full Jest, env guard, lint, and build. | Unassigned |
| R-014 | Medium | Process | Future audit findings can duplicate or conflict without reconciliation before implementation. | Updated: A-001, A-002, A-003, A-004, A-006, A-007, A-008, A-012, A-013, A-014, A-015, A-016, and A-019 reconciled on 2026-05-14. A-009, A-010, A-020, and A-021 were reconciled on 2026-05-18. A-011, A-017, and A-018 were reconciled on 2026-05-19 into F-091 through F-104 plus existing finding updates, risk tracker updates, workstream backlogs, and ready task briefs T-141 through T-155. T-141/T-142 and T-144 through T-155 have since been reconciled back into shared trackers. A-005 was reconciled on 2026-05-22 into F-105 through F-110, R-032/R-033, the frontend workstream backlog, and ready task brief T-199. A-022 was reconciled on 2026-05-23 into F-111 through F-117, R-012/R-019/R-025, the rendering architecture doc, workstream backlogs, and planned task briefs T-230 through T-235. | Unassigned |
| R-015 | High | Forms/Data | Public, user, and admin input flows have server-side validation, normalization, accessibility, and persistence gaps. | Mitigated for the reconciled A-016 scope: T-010 resolved public enquiry validation, T-012 resolved user comment create/update validation, T-013 resolved sign-in credentials wiring plus sign-in accessibility, T-017 resolved subscription validation, T-020/T-034/T-037/T-038 resolved admin collection/article/artwork/blog create/update validation, T-021/T-035/T-036 resolved public search/artwork/shop query bounds, T-053 through T-056 resolved the scoped article/blog/user field-contract input follow-ups, T-104 resolved public search/drawer/mobile navigation and unauthenticated artwork intent control semantics, and T-105 resolved comment action control semantics. | Unassigned |
| R-016 | Medium | Discovery | Search, navigation, taxonomy, and content discovery have confirmed production gaps after A-017 and A-018. | Partially mitigated: A-017 and A-018 are complete and reconciled. T-145 aligned `/artwork` page query parsing/defaults with the public artwork API schema. T-146 preserved sorted blog loading through follow-up requests and rendered sorted pagination links. T-147 made main navigation resilient with route-root fallbacks when dynamic nav data is missing or unavailable. T-151 added current-scope search no-results states and selected-type pagination for articles, blogs, and collections. T-153 exposed blog pinned/tag controls and data-derived admin blog year filters. T-154 added route-local section validation for public article/navigation and collection list inputs. T-155 made visible breadcrumbs content-aware on targeted detail routes. T-210 added MongoDB-backed artwork results to public search, and T-211 added Shopify product results through the existing public shop product-list data path. Remaining discovery gaps include language/i18n launch direction, collection section launch policy, and footer/legal cleanup under R-018. | Unassigned |
| R-017 | High | Dependencies | Production dependency health and supply-chain controls are not production-ready. | Partially mitigated: T-011 removed the critical production audit exposure and patched current-major A-019 advisories. T-016 removed the bcrypt production advisory path by upgrading to `bcrypt@6.0.0`. T-022 removed unused direct dependency surface and the unused `next-test-api-route-handler -> core-js` install-script path. The Vercel bcrypt incident is resolved in production through output-file tracing plus T-023's public import-boundary change. T-064 pinned npm, the Node 22 runtime policy, root Node version files, npm engine enforcement, lockfile metadata, and `npm ci` install discipline. Native bcrypt install/build policy for future runtime changes, CI/dependency-update automation, and the residual Next/PostCSS decision remain separate. | Unassigned |
| R-018 | High | Compliance | Privacy, consent, and commerce compliance gaps require owner/legal review before production launch. | Blocked on owner input for any further runtime work: A-020 inventoried the gaps. T-100 mitigated the product enquiry context slice by persisting normalized Shopify product handles in enquiry submissions. T-208 added a hosted Shopify purchase handoff, but does not create sale, payment, shipping, refund, or buyer-protection policies. T-209 removed unsupported commerce assurance claims from shared public banners and matching security translation files. T-212 prepared the owner/legal decision packet and recorded Heron Laoutaris / hlaoutaris@gmail.com as the approved owner/privacy contact, with recommendations approved for implementation scoping. T-213 added public `/privacy` and `/terms` routes, footer legal links, owner approval version display, cookie/session/third-party disclosure, and factual Shopify-hosted handoff boundary copy. T-214 added newsletter consent/source metadata and public unsubscribe behavior. T-215 added account privacy/terms acknowledgement metadata for credentials and OAuth-created users, an app-owned provider sign-in notice, and a manual delete/export/correction request handoff. T-216 added public comment posting notice, privacy/terms links, and a manual moderation/removal/correction handoff. T-217 added contact/product and artwork enquiry privacy/retention notices with manual privacy/legal handoff. T-218 removed footer social placeholders and stale copyright. T-219 records the remaining owner inputs: Shopify policy target URLs, real social URLs if they should be shown, launch jurisdiction/audience assumptions before jurisdiction-specific legal claims, and explicit approval before future self-service privacy or comment moderation/reporting workflows. | Blocked pending owner inputs after T-219 |
| R-019 | Medium | Observability/Ops | Monitoring, alerting, rollback ownership, smoke evidence, and production logging/redaction policy are not fully defined or automated. | Partially mitigated: T-025 defined deployment smoke evidence, targeted Vercel log requirements, owner-provided log excerpt fallback, rollback triggers, and `npm run smoke:public`. T-041 through T-095 removed direct/debug `console.log()` paths and added public-safe internal-failure bodies across many route slices. T-099 added request ID propagation/generation, structured redacted API logging, and optional public `requestId`/`X-Request-Id` response helper support for a representative route slice. T-116 added the incident-response runbook with severity, triage, rollback/defer/mitigate rules, evidence redaction/retention, owner matrix placeholders, and post-incident follow-up. T-117 through T-121 migrated public, protected user, and admin API internal-failure logging. T-122 guards the route-level API source invariant. T-123 added the provider-neutral monitoring/error-reporting architecture plan and implementation contract. T-124 made unauthenticated public smoke CI-accessible through manual and scheduled GitHub Actions runs once non-secret repository variables are configured. T-125 defined the lower-level service/client logging policy and migration inventory, finding 86 direct non-route `console.error()`/`console.warn()` calls across 66 files. T-126 added a requestless server structured logger and migrated the first public loader/App Router page slice to redacted structured events with focused source hygiene. T-127 migrated scoped Shopify provider/data service failure logging to structured redacted server events with focused source hygiene. T-128 migrated scoped server action/session-helper failure logging to structured redacted server events with focused source hygiene. T-129 migrated the remaining account saved-artwork server loader failure path to structured redacted server events with focused source hygiene. T-130 removed scoped public browsing client console failures and added source-hygiene plus behavior coverage for existing UI state, modals, and loading/fallback behavior. T-131 removed scoped account/user client console failures and added source-hygiene plus behavior coverage for contact/comment/logout/account-nav/comment-card/error-boundary failure state. T-132 removed scoped shared fetcher and low-value utility/helper console failures and added source-hygiene plus fallback behavior coverage. T-133 removed the remaining known admin-dashboard client direct console failures and added recursive admin dashboard source hygiene; the known non-route direct `console.error()`/`console.warn()` inventory is clear except for the approved structured logger sink. T-134 replaced raw incident owner placeholders with explicit blocked owner-decision rows, interim owner/orchestrator escalation, next owner actions, and authority boundaries for rollback, release changes, provider checks, data restore, credential rotation, and privacy/legal communication. T-139 recorded ADR 0005, making the unresolved monitoring provider/no-provider launch decision durable and dated. A-022 confirmed this remains a Next.js feature-utilization gap because there is still no `instrumentation.ts`, web-vitals reporting, monitoring SDK, OpenTelemetry, or provider-backed production measurement. Remaining gaps include owner/platform provider approval or explicit no-provider launch policy, monitoring/error-reporting SDK or instrumentation, alert automation, owner-approved role assignments for the blocked matrix rows, credentialed/admin smoke, Vercel log inspection, source-map/release tracking, and provider-backed monitoring. | Unassigned |
| R-020 | High | Shopify | Product detail pages fetch linked artwork from a non-existent artwork API path. | Mitigated: T-007 replaced the product detail linked-artwork fetches with the server-only `getArtworkById` data path and added focused service/API/page tests. | Unassigned |
| R-021 | High | Admin/Shopify | Admin Shopify product-linking workflow and validation are missing. | Mitigated: T-082 added strict admin artwork create/update route validation for optional `shopifyProducts` writes, requiring numeric product IDs, known product types, and no within-artwork duplicate product IDs. T-097 added the visible admin artwork form workflow for adding, editing, removing, and clearing canonical Shopify product links with shared form-schema validation and focused tests. | Unassigned |
| R-022 | High | Security | A removed Shopify credential-like source comment may require owner verification or rotation. | Partially mitigated: T-009 removed the token-shaped source comment and added a regression check. Owner verification of whether the removed value was real, and rotation if needed, remains open until confirmed. | Unassigned |
| R-023 | Medium | Data/Shopify | Shopify product IDs are documented as numeric but not fully validated or migrated. | Mitigated: T-004 validates numeric IDs for the public single-product route, and T-057 centralizes public product ID normalization/GID construction across public product reads. The listing route skips malformed stored IDs before Shopify fan-out and deduplicates after normalization. T-058 added the read-only MongoDB audit, and T-059 completed it against the owner-approved `laoutarisDB` target with 0 invalid IDs, 0 unknown types, 0 within-artwork duplicates, and 1 review-only cross-artwork book duplicate group. T-082 recorded the shared book-link policy and made admin artwork writes reject malformed IDs, unknown/missing types, and within-artwork duplicate product IDs before persistence. No product-ID cleanup or migration is indicated. | Unassigned |
| R-024 | High | Deployment/Quality | Build and runtime verification are coupled to external services and deployment-specific native/runtime behavior. | Partially mitigated: A-007 found sandbox build failure on MongoDB DNS/egress and external-access build success that still used live MongoDB. The Vercel bcrypt incident showed local Jest/lint are insufficient for native serverless bundle behavior. T-024 resolved the bcrypt crash, and T-025 made runtime smoke, credentials smoke handling, targeted Vercel logs, and rollback evidence repeatable. External-service build coupling and future runtime/package changes still need targeted verification. | Unassigned |
| R-025 | High | Architecture | Client/server import boundaries can pull server-only or model modules into client components. | Mitigated: T-137 added a static guard that discovers `"use client"` entries, walks runtime local imports and re-exports, stops at explicit `"use server"` action boundaries, and fails when the client runtime graph reaches server-only imports, DB/model/data-service files, server session/auth helpers, data type modules through value imports, or known mixed barrels. T-205 cleaned scoped mixed component barrel imports from server routes/loaders. T-230 repaired the A-022 regression by replacing `SignUpForm`'s broad `@/lib/constants` runtime import with the narrow account privacy acknowledgement constants import; focused import-boundary/sign-up privacy tests and build passed. | Unassigned |
| R-026 | High | Data/API | MongoDB-backed API routes and account actions can access models without explicit service or wrapper DB connection ownership. | Partially mitigated: T-027 added explicit `dbConnect()` ownership before user saved read-route model reads while preserving saved-route DTOs. T-028 added explicit `dbConnect()` ownership and route revalidation to favourite/watchlist server actions. T-039 added explicit target-read `dbConnect()` ownership to admin read routes while preserving shared admin-role verification ownership in `requireApiAdmin()`. T-040 added route-local `dbConnect()` ownership before admin delete target model writes and transactions. T-046 added explicit route-local DB ownership to public collection routes, T-047 added missing route-local DB ownership to public collection navigation detail/artworks routes, T-076 moved public blog list DB ownership into `getBlogList` shared by the API route and loaders, T-078 moved public collection artwork list/detail DB ownership into shared services, T-079 moved public article list DB ownership into `getArticleList` shared by the API route and `BiographySectionLoader`, T-080 moved public collection list DB ownership into `getCollectionList` shared by the API route and `CollectionSectionLoader`, T-081 moved user account navigation DB ownership into `getOwnUserNavigation` shared by the route and loader, T-083 moved user saved-artwork read DB ownership into `getOwnSavedArtwork` shared by the protected routes and account loaders, T-084 moved user profile/comment read DB ownership into `getOwnUserProfile` and `getOwnUserComments` shared by the protected routes and account loaders, and T-085 moved public shop product-list DB ownership into `getShopProductList` shared by the public route and loader. A-002's broader route inventory remains open through data/API, architecture, and auth workstreams. | Unassigned |
| R-027 | High | Deployment/Security | Server-only secrets can be exposed through Next config `env`. | Mitigated: T-001 removed `MONGO_URI` from `next.config.mjs`; T-006 added `npm run env:guard`, build-time guard execution, and focused tests for known server-only and secret-like env names. | Unassigned |
| R-028 | Medium | Deployment/Ops | Deployment runtime, Vercel project settings, smoke checks, and rollback steps are not fully owned or automated. | Partially mitigated: T-025 standardized deployment ID/runtime evidence fields, Vercel log access requirements, credentials smoke handling, rollback triggers, and added `npm run smoke:public` for unauthenticated route status checks. T-064 committed repository runtime/package-manager pins and `npm ci` install docs. T-124 added a GitHub Actions public-smoke workflow with manual `base_url` input, scheduled runs gated by non-secret repository variable `SMOKE_BASE_URL`, Node `22.14.0`, `npm ci`, and optional non-secret `SMOKE_*` route inputs. T-134 documented that Vercel rollback/log ownership is blocked until the owner/orchestrator identifies the Vercel-access operator and backup for an incident. Credential/admin/log checks, rollback automation, owner-approved Vercel project-setting ownership, and permanent rollback authority assignment remain open under the deployment workstream. | Unassigned |
| R-029 | High | Auth/Security | Registration logging exposes credential material and user documents. | Mitigated: T-009 removed direct registration console logs and added focused source hygiene coverage for the regression. Broader logging policy remains tracked separately under R-019/F-053. | Unassigned |
| R-030 | High | SEO/Discovery | Public archive, article, blog, and shop pages lack production metadata and crawler discovery signals. | Mitigated: T-103 replaced scaffolded root metadata with production-safe archive metadata and added baseline `robots.ts`/`sitemap.ts` discovery files for stable public routes. T-106 added route-specific metadata, canonical/social previews, and conservative JSON-LD for public biography article and blog detail pages. T-107 added route-specific metadata, canonical/social previews, and conservative JSON-LD for standalone artwork, collection-scoped artwork, and Shopify product detail pages without commerce claims. T-112 added conservative breadcrumb JSON-LD to high-value detail pages. T-113 expanded sitemap output with best-effort dynamic public detail URLs. T-114 added deployed discovery endpoint smoke assertions for `/robots.txt` and `/sitemap.xml`. | Unassigned |
| R-031 | High | Accessibility | Public search, drawer, mobile navigation, and artwork intent controls are not consistently keyboard-accessible labelled controls. | Mitigated: T-104 converted the public search submit, search drawer trigger/close, mobile navigation drawer trigger/close, and unauthenticated favourite/watchlist controls to labelled semantic buttons while preserving search URLs, drawer behavior, navigation links, authenticated saved-item forms, and tooltips. Focused Jest, lint, build, and `git diff --check` passed. | Unassigned |
| R-032 | Medium | Frontend/Rendering | Client follow-up fetch fallback states can produce stale UI without clear user recovery. | Mitigated for the reconciled A-005 fallback scope: T-199 through T-202 resolved the public detail route not-found/error contract across artwork, collection-scoped artwork, article/blog, and Shopify product detail pages; T-203 resolved home section loader blank fallbacks with visible unavailable/empty states; T-204 added visible retry/recovery states for artwork filter/load-more, shop product filter, and blog continuous-loading client fetch failures while preserving existing content; and T-207 documented the accepted route fallback pattern while replacing the remaining `/project/aims` generic inline loading fallback. | Unassigned |
| R-033 | Medium | Architecture | Mixed component barrels remain available to server callers and can reintroduce client/server boundary risk if those callers become client entries. | Mitigated for the scoped A-005 server route/loader finding: T-137 mitigated the current client runtime graph with a recursive import-boundary guard and no allowlist, and T-205 replaced the remaining scoped `src/app` and `src/components/loaders` value imports from `@/components/sections`, `@/components/views`, and `@/components/loaders/viewLoaders` with direct file imports plus source-hygiene coverage. The barrel files still exist for current non-scoped uses and should not be deleted outside a separate source-pruning task. | Unassigned |
| R-034 | High | Quality | Verification gates are not release-ready in the current recalibrated worktree. | Partially mitigated: A-027 found full Jest red, explicit TypeScript red, build coupled to external access, and no main CI workflow. T-263 removed stale auth/observability verification drift; T-268, T-275, and T-285 restored explicit TypeScript `noEmit`; T-276, T-277, and T-280 repaired the full-Jest blocker classes; T-282 rebaselined local verification with full Jest, build, lint, and whitespace passing under Node `22.14.0`; and T-286 added repo-owned `npm run typecheck` and `npm run verify:local` scripts. Main CI, external-access release evidence, scheduled/detail public smoke, credentialed/admin smoke, Vercel log evidence, and other owner/platform-blocked production operations remain separate under F-126/R-038. | Unassigned |
| R-035 | High | Deployment/Build | Production build evidence still depends on external data access and accidental build-time route coupling. | Partially mitigated by routing: A-027 and A-035 confirmed default-sandbox `npm run build` fails on MongoDB SRV DNS while external-access build passes. T-278 mitigated accidental `/account*` and `/project/about` hard failures, T-279 documented required external-access build and sitemap/redirect smoke evidence for intentional `/biography`, `/collections`, and `/sitemap.xml` surfaces, T-283 removed live biography/collection navigation reads from the global root header, and T-284 made `/prototype/home` explicitly dynamic so prototype section loaders no longer run during production static generation. | Unassigned |
| R-036 | Medium | Admin Operations | Recalibration found admin delete and content-control gaps, most of which are now mitigated but some media/discovery workflows remain manual. | Partially mitigated: T-264 fixed A-030 reciprocal delete-reference cleanup for artwork/user saved arrays and collection/artwork links; T-271 added admin artwork replacement-image upload through the existing signed Cloudinary path; T-272 added visible article/collection artwork relationship lookup, duplicate, ready, unchanged, and no-op feedback; T-273 added persisted-role frontend admin guarding. Remaining admin-control gaps are lower-priority blog/collection image picker or pre-submit image feedback and optional comment/user search filters for operator friction. | Unassigned |
| R-037 | Medium | Public Browse/Search/Shop | Public archive and shop runtime hotspots from A-028 are mostly mitigated, with owner-review commerce preview decisions still open. | Partially mitigated: T-265 fixed deep-linked artwork browse pagination state; T-266 removed untyped public-search Shopify product-list fan-out by making shop products explicit-only; T-269 made shop sorting route-backed and filter options canonical; T-270 rendered plain product descriptions as text and mapped missing collection redirect targets to not-found behavior. T-261 sale-gallery frame/mat/gallery controls remain preview-only and need owner visual review before purchase, enquiry, variant, price, or persistence behavior changes. | Unassigned |
| R-038 | Medium | Observability/Ops | Production operations blockers remain owner/platform-gated after A-033. | Open: A-033 reconfirmed no approved monitoring provider/no-provider launch posture, no provider instrumentation, no completed incident/Vercel operator matrix, no approved credentialed smoke accounts, no Vercel log-access evidence path, and no scheduled/detail public-smoke repository variable configuration. T-274 added the production ops owner decision packet to collect those answers; implementation remains blocked until owner/orchestrator answers exist. | Unassigned |

## Owner Rule

The orchestrator is the default owner for `Unassigned` risks until a specific
owner is recorded.

## Update Rules

- Keep each risk short and actionable.
- Link detailed work to the relevant workstream.
- Add a new row when a task uncovers unresolved production risk.
- Mark mitigation status with evidence when closing a risk.
